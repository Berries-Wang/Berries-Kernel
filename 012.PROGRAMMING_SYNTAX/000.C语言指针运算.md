# C 语言指针运算
### 在 C 语言中，两个同类型指针相减，结果是它们之间相隔的元素个数（而不是字节数）。这里就是 zone 指针与数组首地址之间相隔的 struct zone 元素个数
- 示例代码: [000.SOURCE_CODE/000.LINUX-5.9/000.LINUX-5.9/include/linux/mmzone.h](../000.SOURCE_CODE/000.LINUX-5.9/000.LINUX-5.9/include/linux/mmzone.h)#`#define zone_idx(zone)		((zone) - (zone)->zone_pgdat->node_zones)`