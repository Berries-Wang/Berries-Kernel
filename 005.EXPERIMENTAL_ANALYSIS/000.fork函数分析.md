# _do_fork返回两次分析
```c
/**
 *  000.SOURCE_CODE/000.LINUX-5.9/000.LINUX-5.9/kernel/fork.c
 * 
 * fork函数
 */
#ifdef __ARCH_WANT_SYS_FORK
SYSCALL_DEFINE0(fork)
{
#ifdef CONFIG_MMU
	struct kernel_clone_args args = {
		.exit_signal = SIGCHLD,
	};

	return _do_fork(&args);
#else
	/* can not support in nommu mode */
	return -EINVAL;
#endif
}
#endif
```
子线程从哪里开始运行的呢?   还是得分析程序计数器是什么